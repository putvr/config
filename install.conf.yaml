- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: .zshrc
    ~/.gitconfig: .gitconfig
    ~/.gitigniore_global: .gitigniore_global

- shell:
    - [git clone https://github.com/ohmyzsh/ohmyzsh.git ~/.oh-my-zsh]
